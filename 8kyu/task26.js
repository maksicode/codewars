// We need a function that can transform a number (integer) into a string.

// function numberToString(num) {
//     return String(num);
//   }

const numberToString = num => String(num);

// const total = (numberToString(123));
// console.log(total);
// console.log(typeof total);